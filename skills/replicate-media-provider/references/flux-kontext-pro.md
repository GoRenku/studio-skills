# FLUX Kontext Pro

Use for source-guided image generation and editing. Author `prompt` and put the
exact source marker in the live schema's image input field. Use only native
aspect-ratio, output-format, safety, and seed options returned by current
Replicate metadata. State preserved and changed details explicitly.
