# Veo 3.1 Fast

Use the current Replicate schema to distinguish text-only, first-frame, or
first/last-frame input. Put local markers only at those native fields. Prompt
subject action, camera motion, environment change, timing, and requested native
audio. Do not invent a reference mode the schema does not expose.
