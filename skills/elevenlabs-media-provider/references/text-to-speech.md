# ElevenLabs Text to Speech

Required native fields are `text` and `voice`. The review `model` is the exact
ElevenLabs model id. Optional `voice_settings` may include deliberately chosen
stability, similarity boost, style, speed, and speaker boost values. Optional
`output_format` controls the downloaded audio format. Do not rewrite dialogue
or performance direction without user approval.
