# ElevenLabs Music

Required native field is `prompt`. Optional fields supported by this runtime are
`music_length_ms` and `force_instrumental`. Keep musical form, instrumentation,
mood, tempo, vocals, and narrative timing in the prompt. This route produces a
new composition and accepts no local-media marker.
