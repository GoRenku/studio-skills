# Fal.ai Image Creation

Use the selected model's live validated schema as the final field authority.
Author `prompt` plus only deliberately chosen native options such as image size,
aspect ratio, output format, quality, seed, or output count. Do not guess an
option from a different model. Image references change this into an edit route.
