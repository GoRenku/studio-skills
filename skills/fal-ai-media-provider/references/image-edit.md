# Fal.ai Image Editing

Use the exact `/edit` model. Put each accepted source/reference marker in the
native image URL field or ordered image URL array required by the selected live
schema. State what must remain unchanged and what may change in `prompt`.
Preserve input order; never infer identity from a filename.
