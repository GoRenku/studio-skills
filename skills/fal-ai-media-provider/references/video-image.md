# Fal.ai Image-to-Video

Put the exact starting image marker at the model's native image field. Add an
ending image only when the selected schema has a distinct supported field.
Prompt motion and change over time rather than redescribing a static frame.
