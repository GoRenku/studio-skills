# MiniMax H3 Image-to-Video

Put the starting image marker in the exact native image field. Use a last-frame
field only when the current schema exposes it. Prompt motion and temporal change;
do not ask the model to redraw the source frame.
